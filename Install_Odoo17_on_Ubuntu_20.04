Update & Upgrade Ubuntu Server
sudo apt-get update && sudo apt-get upgrade

Install Database Postgresql
sudo apt install postgresql -y

Install Secure Server
sudo apt-get install openssh-server fail2ban



install odoo from Repository Github
wget -q -O - https://nightly.odoo.com/odoo.key | sudo gpg --dearmor -o /usr/share/keyrings/odoo-archive-keyring.gpg
sudo echo 'deb [signed-by=/usr/share/keyrings/odoo-archive-keyring.gpg] https://nightly.odoo.com/18.0/nightly/deb/ ./' | sudo tee /etc/apt/sources.list.d/odoo.list
sudo apt-get update && sudo apt-get install odoo

